# Database
db.url = jdbc:sqlite:hrapp.db

# Logging
log.dir = ${user.home}/hrapp-logs

# App UI
app.title = HR App
app.width = 1100
app.height = 700
divider.position = 0.35

# Grades
grade.min = 1
grade.max = 10

# Limits
max.name.length = 100
max.skill.length = 100
max.task.name.length = 200
max.comment.length = 500
